package:
  name: munch
  version: "2.0.2"

source:
  fn: munch-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/munch/munch-2.0.2.tar.gz
  md5: 6bff44f4f33b0af4f6f991a996f5a314
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - munch = munch:main
    #
    # Would create an entry point called munch that calls munch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - munch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Infinidat/munch
  license: MIT License
  summary: 'A dot-accessible dictionary (a la JavaScript objects)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
